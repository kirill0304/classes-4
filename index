
Git Manual

    git config --global user.email ""

    Sets commit email address. 

    git diff --staged

    Show changes between commits, commit and working tree, etc. --staged views the changes you staged for the next commit relative to the named . If you do not give , it defaults to HEAD. 

    git checkout -b

    Creates a new branch in local repository and switches to it. 

    git log --graph --all

    git-log - Show commit logs --graph - Draw a text-based graphical representation of the commit history on the left hand side of the output. --all - Pretend as if all the refs in refs/, along with HEAD, are listed on the command line as . 

    git branch -d "branch"

    Delete a branch. The branch must be fully merged in its upstream branch, or in HEAD 

    git fetch "remote" "branch"

    Download objects and refs from another repository 

